import React from "react";
import { useEffect } from "react";

const SubscribeButton = (arg) => {


    return (
        <a href={arg}>
            <button>
                Subscribe to service
        </button>
        </a>
        
    )
}
export default SubscribeButton